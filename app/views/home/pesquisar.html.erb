<div class='container'>
  <div class='row'>
    <ul class="list-group">
      <%= form_tag(new_place_path, method: :get) do %>
        <% @places.each do |place| %>
          <li class="list-group-item"> <%= "#{place.name} - Endereço: #{place.formatted_address}" %>
            <%= hidden_field_tag 'place_id', place.place_id %>
            <%= submit_tag "Adicionar", class: 'btn btnRaid btn-primary pull-right' %>
          </li>
        <%end%>
      <%end%>
    </ul>
  </div>
</div>
